# Gets outside temp from forcast entity
- name: outside_temp
  unit_of_measurement: '°F'
  state: > 
    {{ state_attr('weather.forecast_booth_st_lower', 'temperature') | float}}
# Gets outside humidity from forcast entity
- name: outside_humidity
  unit_of_measurement: '%'
  state: >
    {{ state_attr('weather.forecast_booth_st_lower', 'humidity') | float}}
      
- name: warmest_space
  state: >
    {% set max = states('sensor.max_indoor_temperature') %}
    {% set kitchen = states('sensor.2354_kitchen_t_h_sensor_temp') %}
    {% set dining = states('sensor.2354_dining_room_t_h_sensor_temp') %}
    {% set living = states('sensor.2354_living_room_t_h_sensor_temp') %}
    {% set e_bed = states('sensor.2354_e_bedroom_t_h_sensor_temp') %}
    {% if (kitchen == max) %}
      Kitchen
    {% elif (dining == max) %}
      Dining Room
    {% elif (living == max) %}
      Living Room
    {% elif (e_bed == max) %}
      Bedroom
    {% else %}
      Error, no sensor matches max temp
    {% endif %}
    